box:
  id: criff/docker-goapp-glide
  cmd: /bin/sh
build:
  steps:
    - setup-go-workspace:
      package-dir: gae-go-werker

    - script:
        name: glide
        code: |
          pwd
          glide install
          ls -al

    - script:
        name: go test
        code: |
          pwd
          ls -al
          go test ./...

