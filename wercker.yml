box:
  id: criff/docker-goapp-glide
  cmd: /bin/sh
build:
  steps:
    - setup-go-workspace

    - script:
        name: glide
        code: |
          glide install

    - script:
        name: go test
        code: |
          go test ./...

