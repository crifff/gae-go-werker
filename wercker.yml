box:
  id: criff/docker-goapp-glide
  cmd: /bin/sh
build:
  steps:
    - setup-go-workspace:
      package-dir: gae-go-werker

    - script:
        name: glide
        code: |
          glide install

    - script:
        name: go test
        code: |
          goapp test ./...

